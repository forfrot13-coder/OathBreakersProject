# =====================================================================
# RENDER DEPLOYMENT CONFIGURATION
# =====================================================================
# For Render.com deployment, set these environment variables in the Render dashboard.
# Render automatically provides: DATABASE_URL and RENDER_EXTERNAL_HOSTNAME

# =====================================================================
# REQUIRED: SECRET KEY
# =====================================================================
# Generate with: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
# On Render: This will be auto-generated if using render.yaml
SECRET_KEY=your-secret-key-here-change-this

# =====================================================================
# DEBUG MODE
# =====================================================================
# Set to False in production (Render default: False)
DEBUG=False

# =====================================================================
# ALLOWED HOSTS
# =====================================================================
# Optional: Comma-separated list of additional allowed hosts
# Render automatically adds RENDER_EXTERNAL_HOSTNAME
ALLOWED_HOSTS=

# =====================================================================
# DATABASE
# =====================================================================
# Render automatically provides DATABASE_URL for PostgreSQL
# Format: postgres://user:password@host:port/dbname
# Leave empty for local SQLite development
DATABASE_URL=

# =====================================================================
# CORS CONFIGURATION
# =====================================================================
# Comma-separated list of allowed CORS origins (e.g., https://yourdomain.com)
# Render URL is automatically added
CORS_ALLOWED_ORIGINS=

# =====================================================================
# CSRF TRUSTED ORIGINS
# =====================================================================
# Comma-separated list of trusted origins for CSRF (e.g., https://yourdomain.com)
# Render URL is automatically added
CSRF_TRUSTED_ORIGINS=

# =====================================================================
# SSL/SECURITY (Production)
# =====================================================================
# Set to False if you want to disable SSL redirect
SECURE_SSL_REDIRECT=True
